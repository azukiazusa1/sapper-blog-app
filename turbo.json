{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".svelte-kit/**", ".vercel/**", "build/**"],
      "dependsOn": ["^build"]
    },
    "lint": {},
    "test": {},
    "dev": {
      "cache": false
    },
    "typecheck": {
      "dependsOn": ["build"]
    },
    "format": {}
  }
}
